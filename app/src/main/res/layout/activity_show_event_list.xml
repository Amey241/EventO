<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.evento.ShowEventList"
    android:layout_gravity="start">

    <!-- Main content layout -->
    <RelativeLayout
        android:id="@+id/activity_show_event_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:title="Event List"
            app:titleTextColor="@android:color/white" />

        <!-- RecyclerView for event cards -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerview_event_cards"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar"
            android:paddingStart="8dp"
            android:background="@color/darkred"
            android:clipToPadding="false"
            android:paddingTop="15dp" />

    </RelativeLayout>

    <!-- NavigationView -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@color/white"
        app:headerLayout="@layout/navigation_header"
        app:menu="@menu/navigation_menu"
        android:visibility="visible" /> <!-- Initially set to gone -->

</androidx.drawerlayout.widget.DrawerLayout>
